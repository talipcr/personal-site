import { FormatItemDirective } from './format-item.directive';

describe('FormatItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatItemDirective();
    expect(directive).toBeTruthy();
  });
});
